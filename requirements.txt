# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — pinned runtime dependencies for reproducible Docker builds
# Generated from pyproject.toml; update with: pip-compile pyproject.toml
# ─────────────────────────────────────────────────────────────────────────────

# Telegram bot framework
aiogram==3.27.0

# Database — async runtime
sqlalchemy[asyncio]==2.0.40
asyncpg==0.31.0

# Database — Alembic sync migrations (psycopg2, NOT asyncpg)
alembic==1.15.2
psycopg2-binary==2.9.10

# Crypto Pay: no SDK — we use direct aiohttp calls (see providers/crypto_pay.py)
# Removed aiocryptopay to eliminate certifi<2024 version constraint (CVE-2024-39689)

# Exchange adapters
ccxt==4.5.51

# Structured logging
structlog==25.5.0

# Encryption
cryptography==48.0.0

# Web3 wallet key generation + EVM JSON-RPC client
# web3 includes eth-account as a dependency (compatible version resolved automatically)
web3==7.12.0
# pytoniq: TON mnemonic + key derivation (pure Python, no C deps)
pytoniq==0.1.43

# Config / secrets
python-dotenv==1.2.2

# Webhook HTTP server
aiohttp==3.13.5

# Branding / Configuration
pyyaml==6.0.2

# AI Mediator
google-generativeai==0.8.6

# Security & XML
defusedxml==0.7.1
# certifi: aiocryptopay removed — certifi now unrestricted. CVE-2024-39689 patched.
certifi==2026.4.22
# protobuf: 5.29.6 is max version compatible with google-generativeai (requires <6.0.0dev)
# CVE-2024-51547 patched. Cannot use 7.x — google-api-core pins protobuf<7.0.0.
protobuf==5.29.6
